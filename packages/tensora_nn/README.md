# tensora_nn

Canonical neural-network namespace for Tensora.

Implemented today: native-backed `Module`, `Linear`, and `Losses` APIs already provided by the Tensora runtime. This package exposes those APIs from a focused import without duplicating native bindings.
