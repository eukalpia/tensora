# tensora_optim

Canonical optimizer namespace for Tensora.

Implemented today: native-backed `SGD`, `Adam`, and `AdamW` with deterministic native-handle ownership. The package provides a focused import while native implementation remains owned by `tensora`.
