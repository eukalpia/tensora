import 'package:tensora_optim/tensora_optim.dart';
import 'package:test/test.dart';

void main() {
  test('optimizer API types are exported', () {
    final List<Optimizer> values = <Optimizer>[];
    expect(values, isEmpty);
  });
}
