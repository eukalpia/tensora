# Tensora compiler

This directory owns future graph capture, Tensora IR, shape and type propagation, optimization passes, memory planning, backend partitioning, and compiled execution.

No compiler capability is claimed in the current development line. Executable compiler code belongs here only together with graph/IR correctness tests and workload evidence that justifies the optimization.
