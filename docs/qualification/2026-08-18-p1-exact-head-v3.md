# P1 Exact-Head Qualification V3

This commit starts the complete hosted qualification matrix after guarding optional Windows white-box targets and removing temporary repair workflows.

The pull request remains draft until every required workflow succeeds on this exact head, coverage thresholds are restored, and the result is recorded with its immutable SHA.
